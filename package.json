{
	"private": true,
	"name": "timber",
	"version": "0.1.0",
	"scripts": {
		"install": "pnpm i -r --filter=timber.*",
		"code:scanner": "sonar-scanner",
		"serve": "pnpm run -r serve --filter=timber.*",
		"serve:dev": "pnpm run -r serve:dev --filter=timber.*"
	},
	"devDependencies": {
		"pnpm": "^3.6.0",
		"sonar-scanner": "^3.1.0",
		"typescript": "^3.6.3"
	}
}
